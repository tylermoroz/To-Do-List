const todo = (title, description, time, priority, notes, checklist) => {

    return { title, 
             description, 
             time, 
             priority, 
             notes, 
             checklist 
            };
};

export default todo;